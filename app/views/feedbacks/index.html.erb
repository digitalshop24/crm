<div class="outerwrapper">
  <!-- =header -->
  
  <%= render 'header' %>
  <!-- =/header -->
  <!-- =floatcontacts -->
  <div class="floatcontacts container">
    <div>
      <a href="tel:+375291111111" class="tel"><span>+375 (29)</span> 111 11 11</a>
      <p class="skype">skype: <a href="skype:RedStudent" class="skype_link">RedStudent</a></p>
    </div>    
  </div>

  <div class="main">
    <div class="container">
      <h1>ОТЗЫВЫ</h1>
      <div class="intro intro_review">
        <p>Lorem ipsum dolor sit amet, consetetur sadipscing elitr, ased diam nonumy eirmod tempor invidunt ut labore et adolore magna aliquyam erat, sed diam voluptua. At avero eos et accusam et justo duo dolores et ea rebum. aStet clita kasd gubergren, no sea takimata sanctus est aLorem ipsum dolor sit amet.</p>
      </div>
      <div class="reviewList">
      <% @feedbacks.each do |feedback| %>
        <div class="item">
          <h3><%=feedback.name%></h3>
          <p><%=feedback.feedback%></p>
          <time datetime="2015-10-22"><%=feedback.created_at.to_s(:db)%></time>
        <% if current_user %>
        <% if "Admin".include? current_user.role %>
          <%= link_to 'Изменить', edit_feedback_path(feedback) %>
          <%= link_to 'Удалить', feedback, method: :delete, data: { confirm: 'Вы уверены?' } %>
        <%end%>
      <%end%>
        </div>
      <%end%>
      </div>
    <%= will_paginate @feedbacks, previous_label: "&laquo;", next_label: "&raquo;", renderer: BootstrapPagination::Rails %>
<br>
<% if current_user %>
 <% if "Admin".include? current_user.role %>
  <%= link_to 'Добавить', new_feedback_path %>
 <%end%>
<%end%>
    </div>
  </div>

  </div>
  <!-- =/floatcontacts -->
 <%= render 'footer' %>



