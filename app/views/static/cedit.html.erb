 
<div class="outerwrapper">
<%= render 'welcome/header' %>

  <div class="floatcontacts container">
    <div>
      <a href="tel:+375291111111" class="tel"><span>+7 (800) </span>775-76-81</a>
      <p class="skype"> Звонок бесплатный</p>
    </div>    
  </div>
  
  <!--InstanceBeginEditable name="EditRegion3" -->
  <div class="main">
    <div class="container">
    <div class="static">
  <div id="myEditor"> <%= render 'contactscontent' %></div>
<button id="saveButton">Save</button>
</div>
</div>
</div>
<script>
  $(function() {
    $('#myEditor')
      .froalaEditor({
        language: 'ru',
        // Set the save param.
        saveParam: 'content',
        // Set the save URL.
        saveURL: "#{ENV['host']}/contacts/edit",
        // HTTP request type.
        saveMethod: 'GET',
        // Additional save params.
        saveParams: {id: 'my_editor'}
      })
      .on('froalaEditor.save.before', function (e, editor) {
        // Before save request is made.
      })
      .on('froalaEditor.save.after', function (e, editor, response) {
        alert('изменения сохранены');
      })
      .on('froalaEditor.save.error', function (e, editor, error) {
        // Do something here.
      })
  });
  $('#saveButton').click (function () {
    $('#myEditor').froalaEditor('save.save')
  })
</script>

 
</div>

<%= render 'welcome/footer' %>