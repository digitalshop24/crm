<% links = {
	my_orders: ["Мои заказы", dashboard_index_path],
	new_orders: ["Новые заказы", dashboard_new_orders_path],
	profile: ["Профиль", edit_user_registration_path],
	account: ["Личный счет", show_account_path]
} %>
<div class="btn-toolbar">
  <div class="btn-group">
    <% links.each do |key, value| %>
      <%= link_to value[0], value[1], class: 'btn btn-default btn-lg' %>
    <% end %>
  </div>
</div>
<br>
